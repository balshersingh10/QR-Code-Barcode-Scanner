export namespace AppState{
    export var url = {
        myValue: 'Nothing'
    }
}

//setTimeout(function(){
    //alert({
      //  title: "You Scanned ",
      //  message: "Format: " + result.format + "\nContent: " + result.text,
      //  okButtonText: "OK"
   // });
   // },0);
